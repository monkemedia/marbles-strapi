'use strict';

/**
 * information-navigation service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::information-navigation.information-navigation');
